#include <stdio.h>

int main() 
{
    int num;
    printf("Enter the number");
    scanf("%d",&num);
    if (num>0)
    {
        printf("POSITIVE NUMBER");
    }
    else if(num==0)
    {
        printf("ZERO");
    }
    else
    {
        printf("NEGATIVE NUMBER");
    }
    
    return 0;
}

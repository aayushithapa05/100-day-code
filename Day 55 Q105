#include <stdio.h>

int main() {
    int n, i;
    int a[300];
    scanf("%d", &n);
    for(i = 0; i < n; i++)
        scanf("%d", &a[i]);

    int candidate = 0, count = 0;
    for(i = 0; i < n; i++) {
        if(count == 0) {
            candidate = a[i];
            count = 1;
        } else if(a[i] == c

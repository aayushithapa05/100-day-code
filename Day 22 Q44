#include <stdio.h>

int main()
{
    int num;
    float sum=1;
    printf("Enter the value of n..");
    scanf("%d",&num);
    for (int i=3;i<num*2;i=i+2)
    {
        sum=sum+((float)(i)/(float)(i+1));
    }
    printf("The sum of n Terms is %f",sum);
    
    return 0;
}

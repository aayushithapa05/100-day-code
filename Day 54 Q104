#include <stdio.h>
#include <math.h>

int main() {
    int n;
    scanf("%d", &n);

    double val = n * (n + 1) / 2.0;
    int x = (int)sqrt(val);

    if (x * x == val)
        printf("%d", x);
    else
        printf("-1");

    return 0;
}
